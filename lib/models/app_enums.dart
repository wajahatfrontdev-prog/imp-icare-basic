enum UserRole {
  patient,
  labTechnician,
  doctor,
  pharmacist,
  student,
  instructor,
  unknown,
}

enum BookingStatus { upcoming, cancelled, completed }

enum OrderType { recent, delivered, cancelled, inTransit }
