/// Agora RTC credentials
/// Get App ID from: https://console.agora.io → Your Project → App ID
/// Token is fetched from backend at runtime — do NOT hardcode it here.
class AgoraConfig {
  // Replace with your actual Agora App ID from console.agora.io
  // This is a fallback — token response from backend already includes appId
  static const String appId = '82a63a65663c49f0bb973707b4c09f5f';
}
